<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <a href="#" class="close js-dismiss">×</a>
            <h3 class="modal-title">{{ 'Confirm Deletion' |trans({}, 'ZymSecurityBundle') }}</h3>
        </div>

        <div class="modal-body">
            <p>
                You are about to delete <strong>"{{  aclClass.type |e }}" - "{{ index |e }}"</strong>.
                This action cannot be undone.<br />
                <strong>Are you sure you want to proceed?</strong>
            </p>
        </div>

        <div class="modal-footer">
            <form action="{{ path('zym_security_acl_entries_delete', { 'classType' : aclClass.type, 'index' : index }) }}" method="post" {{ form_enctype(form) }}>
                {{ form_rest(form) }}

                <input class="btn btn-danger" type="submit" value="{{ 'Confirm Deletion' | trans({}, 'ZymSecurityBundle') }}" />
                <button type="button" class="btn btn-default" data-dismiss="modal" aria-hidden="true">{{ 'Cancel' | trans({}, 'ZymSecurityBundle') }}</button>
            </form>
        </div>
    </div>
</div>